# Screenshots

Drop the images referenced by the root `README.md` here:

| Filename | What it should show |
| --- | --- |
| `landing.png` | The MailOra landing page (the "Never miss a career opportunity again" hero). |
| `dashboard.png` | The logged-in dashboard / inbox with classified opportunities. |
| `whatsapp-reminder.png` | A WhatsApp deadline reminder (screenshot or short GIF). |

Keep the filenames exactly as above so the README links resolve.
